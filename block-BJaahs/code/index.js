let title = testData.title;
let options = testData.options;
let correct = testData.correctAnswerIndex;

function isAnswerCorrect(index){
    return function getCorrectAnswer(){
        if(indexOf(options) == correct){

        }
    }
}